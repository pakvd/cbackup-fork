# 🚀 Руководство по чистой установке cBackup

Это руководство поможет вам выполнить чистую установку cBackup с проверкой всех функций, включая синхронизацию `application.properties`.

## 📋 Предварительные требования

### Системные требования
- **Docker** 20.10 или новее
- **Docker Compose** 2.0 или новее
- **Минимум 4GB RAM**
- **Минимум 10GB свободного места**
- **Linux сервер** (Ubuntu 20.04+, Debian 10+, CentOS 7+)

### Проверка установки Docker

```bash
# Проверить версии
docker --version
docker compose version

# Если не установлены, выполните:
curl -fsSL https://get.docker.com -o get-docker.sh
sudo sh get-docker.sh
sudo usermod -aG docker $USER
sudo systemctl enable docker
sudo systemctl start docker

# Установка Docker Compose
sudo curl -L "https://github.com/docker/compose/releases/latest/download/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker compose
sudo chmod +x /usr/local/bin/docker compose
```

## 🔧 Шаг 1: Подготовка окружения

### 1.1 Клонирование репозитория

```bash
# Если репозиторий еще не склонирован
git clone <repository-url>
cd cbackup-fork

# Или если уже есть локальная копия
cd cbackup-fork
git pull origin main  # обновить до последней версии
```

### 1.2 Настройка переменных окружения

```bash
# Создать .env файл из примера
cp .env.example .env

# Отредактировать .env (изменить пароли!)
nano .env
# или
vi .env
```

**Важно:** Измените пароли по умолчанию в `.env`:
```env
MYSQL_PASSWORD=<ваш_сложный_пароль>
MYSQL_ROOT_PASSWORD=<ваш_сложный_root_пароль>
```

### 1.3 Проверка структуры проекта

```bash
# Убедитесь, что все файлы на месте
ls -la
ls -la core/
ls -la worker/
ls -la nginx/

# Проверьте наличие composer.json
ls -la core/composer.json
```

## 🧹 Шаг 2: Очистка предыдущей установки (если была)

```bash
# ⚠️ ВНИМАНИЕ: Это удалит все данные!

# Остановить и удалить контейнеры
docker compose down -v

# Удалить образы (опционально)
docker compose down --rmi all

# Очистить все неиспользуемые данные Docker
docker system prune -a --volumes

# Убедиться, что все очищено
docker ps -a
docker images | grep cbackup
```

## 🏗️ Шаг 3: Установка прав доступа

```bash
# Установить права на файлы
make set-permissions

# Или вручную:
chmod +x set-permissions.sh
./set-permissions.sh

# Проверить, что bin директория создана
ls -la core/bin/
```

**Ожидаемый результат:**
```
✓ Created bin directory
✓ Set permissions for bin directory (755)
✓ Created application.properties with default values
✓ Set permissions for application.properties (644, non-executable)
```

## 🚀 Шаг 4: Запуск контейнеров

```bash
# Запустить все контейнеры
make up

# Или вручную:
docker compose up -d

# Проверить статус контейнеров
docker compose ps

# Просмотреть логи (должны быть без критических ошибок)
docker compose logs --tail=50
```

**Ожидаемый результат:**
- Все контейнеры должны быть в статусе `Up`
- Нет критических ошибок в логах

### 4.1 Автоматическая установка зависимостей Composer

**✅ Теперь автоматически!** Entrypoint скрипт и Makefile автоматически устанавливают зависимости при запуске контейнеров.

Entrypoint скрипт:
- Автоматически проверяет наличие директории `vendor` при старте контейнера
- Если директория отсутствует, автоматически запускает `composer install`
- При ошибках автоматически пробует `composer update`
- Проверяет успешность установки

Makefile (`make up`):
- Автоматически проверяет установку зависимостей после запуска контейнеров
- Если зависимости не установлены, автоматически устанавливает их

**Проверка:**

```bash
# Проверить логи установки зависимостей
docker compose logs web | grep -i composer

# Проверить, что vendor директория создана
make check-deps

# Или вручную:
docker compose exec web test -f /var/www/html/vendor/autoload.php && echo "✓ Зависимости установлены" || echo "✗ Зависимости НЕ установлены"
```

**Если зависимости не установились автоматически (редкий случай):**

```bash
# Установить зависимости вручную
make install-composer

# Или через скрипт:
./QUICK_FIX_COMPOSER.sh
```

## 🔍 Шаг 5: Проверка создания application.properties

### 5.1 Проверка в контейнере

```bash
# Проверить, что файл создан
docker compose exec web ls -la /var/www/html/bin/

# Проверить содержимое файла
docker compose exec web cat /var/www/html/bin/application.properties

# Проверить права доступа
docker compose exec web stat /var/www/html/bin/application.properties
```

**Ожидаемый результат:**
```
-rw-r--r-- 1 www-data www-data 409 Nov  2 14:08 /var/www/html/bin/application.properties
```

### 5.2 Проверка на хосте (если используете volume mounts)

```bash
# Проверить на хосте
ls -la core/bin/

# Проверить содержимое
cat core/bin/application.properties
```

## 🌐 Шаг 6: Веб-установка

### 6.1 Открыть веб-интерфейс

```bash
# Открыть в браузере
http://localhost:8080
# или
http://ваш-сервер:8080
```

### 6.2 Пройти установку

1. **Шаг 1: Настройка базы данных**
   - База данных должна быть уже создана (через Docker)
   - Проверьте подключение
   - Нажмите "Create Tables"

2. **Шаг 2: Создание администратора**
   - Введите логин администратора
   - Введите пароль администратора
   - Введите email
   - Нажмите "Create Admin User"

3. **Шаг 3: Завершение установки**
   - Нажмите "Complete Installation"
   - Должно произойти перенаправление на главную страницу

### 6.3 Проверка после установки

После завершения установки проверьте:

```bash
# Проверить, что install.lock создан
docker compose exec web ls -la /var/www/html/ | grep install.lock

# Проверить application.properties (должен быть синхронизирован с БД)
docker compose exec web cat /var/www/html/bin/application.properties
```

**Ожидаемый результат:** Файл должен содержать значения из базы данных (порт, имя пользователя, пароль).

## ✅ Шаг 7: Проверка синхронизации

### 7.1 Проверка через веб-интерфейс

1. Войдите в систему как администратор
2. Перейдите: `http://localhost:8080/index.php?r=config`
3. Найдите секцию **"Daemon credentials"**
4. Проверьте, что есть кнопка **"Синхронизировать application.properties"**
5. Нажмите кнопку - должно появиться сообщение об успешной синхронизации
6. Страница должна автоматически перезагрузиться
7. Убедитесь, что нет предупреждения "Mismatched data"

### 7.2 Проверка через консольную команду

```bash
# Проверить синхронизацию
docker compose exec web php yii sync-properties/check
```

**Ожидаемый результат:**
```
=== Проверка синхронизации application.properties ===

Значения из базы данных:
  javaSchedulerPort = 8437
  javaSchedulerUsername = cbadmin
  javaSchedulerPassword = <пароль>

Значения из application.properties:
  sshd.shell.port = 8437
  sshd.shell.username = cbadmin
  sshd.shell.password = <пароль>

Сравнение:
  ✅ PORT: БД='8437' == Файл='8437'
  ✅ USERNAME: БД='cbadmin' == Файл='cbadmin'
  ✅ PASSWORD: БД='<пароль>' == Файл='<пароль>'

✅ Все значения совпадают!
```

## 🧪 Шаг 8: Тестирование функциональности

### 8.1 Изменение значений в базе данных

```bash
# Войти в контейнер БД
docker compose exec db mysql -u root -p${MYSQL_ROOT_PASSWORD} cbackup

# В MySQL консоли:
UPDATE config SET value = '8440' WHERE `key` = 'javaSchedulerPort';
UPDATE config SET value = 'testuser' WHERE `key` = 'javaSchedulerUsername';
EXIT;
```

### 8.2 Проверка синхронизации после изменения

1. Откройте страницу конфигурации
2. Должно появиться предупреждение "Mismatched data"
3. Нажмите кнопку "Синхронизировать application.properties"
4. Предупреждение должно исчезнуть
5. Проверьте файл:

```bash
docker compose exec web cat /var/www/html/bin/application.properties | grep -E "(port|username)"
```

Должны быть новые значения: `port=8440`, `username=testuser`

### 8.3 Восстановление значений

```bash
# Вернуть исходные значения
docker compose exec db mysql -u root -p${MYSQL_ROOT_PASSWORD} cbackup -e "
UPDATE config SET value = '8437' WHERE \`key\` = 'javaSchedulerPort';
UPDATE config SET value = 'cbadmin' WHERE \`key\` = 'javaSchedulerUsername';
"
```

## 📊 Шаг 9: Финальная проверка

### 9.1 Проверка всех компонентов

```bash
# Проверить статус всех контейнеров
docker compose ps

# Проверить логи всех сервисов
docker compose logs --tail=20

# Проверить использование ресурсов
docker stats --no-stream
```

### 9.2 Чеклист функциональности

- [ ] Веб-интерфейс доступен
- [ ] Вход в систему работает
- [ ] Страница конфигурации открывается
- [ ] Кнопка "Синхронизировать application.properties" присутствует и работает
- [ ] Файл `application.properties` создан и имеет правильные права
- [ ] Значения в файле соответствуют значениям в БД
- [ ] Консольная команда `yii sync-properties/check` работает
- [ ] Нет предупреждения "Mismatched data"
- [ ] Java worker работает (если проверяется)

## 🔧 Решение проблем

### Проблема: Файл application.properties не создается

**Решение:**
```bash
# Создать вручную
docker compose exec web bash -c "
mkdir -p /var/www/html/bin
cat > /var/www/html/bin/application.properties << 'EOF'
# SSH Daemon Shell Configuration
sshd.shell.port=8437
sshd.shell.enabled=false
sshd.shell.username=cbadmin
sshd.shell.password=
sshd.shell.host=localhost
sshd.shell.auth.authType=SIMPLE
sshd.shell.prompt.title=cbackup

# Spring Configuration
spring.main.banner-mode=off

# cBackup Configuration
cbackup.scheme=http
cbackup.site=http://web/index.php
cbackup.token=
EOF
chown www-data:www-data /var/www/html/bin/application.properties
chmod 644 /var/www/html/bin/application.properties
"
```

### Проблема: Ошибка прав доступа при синхронизации

**Решение:**
```bash
docker compose exec web bash -c "
chown -R www-data:www-data /var/www/html/bin
chmod 755 /var/www/html/bin
chmod 644 /var/www/html/bin/application.properties
"
docker compose restart web
```

### Проблема: Контейнеры не запускаются

**Решение:**
```bash
# Проверить логи
docker compose logs

# Пересобрать образы
docker compose build --no-cache

# Перезапустить
docker compose up -d
```

### Проблема: vendor/autoload.php not found

**Решение:** Установите зависимости Composer:
```bash
# Установить зависимости
docker compose exec web composer install --no-dev --optimize-autoloader --no-interaction --no-scripts --ignore-platform-reqs

# Если не работает, обновить зависимости
docker compose exec web composer update --no-dev --optimize-autoloader --no-interaction --no-scripts --ignore-platform-reqs

# Проверить установку
docker compose exec web test -f /var/www/html/vendor/autoload.php && echo "OK" || echo "ERROR"
```

📖 **Подробнее:** см. [INSTALL_COMPOSER_DEPS.md](INSTALL_COMPOSER_DEPS.md)

## 📝 Примечания

1. **Первая установка**: После первой установки файл `application.properties` будет создан автоматически с значениями по умолчанию из базы данных.

2. **Последующие запуски**: При каждом запуске контейнера `web` entrypoint скрипт автоматически проверяет и исправляет права доступа.

3. **Синхронизация**: Синхронизация происходит автоматически при:
   - Сохранении настроек в веб-интерфейсе
   - Загрузке страницы конфигурации (проверка)
   - Ручном нажатии кнопки синхронизации

4. **Безопасность**: Убедитесь, что файл `.env` не попал в Git и содержит безопасные пароли.

## 🎉 Готово!

После успешного прохождения всех шагов у вас должна быть полностью рабочая установка cBackup с автоматической синхронизацией `application.properties`.

---

**Дата создания:** 2024-11-02  
**Версия:** 1.0

