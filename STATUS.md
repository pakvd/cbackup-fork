# Статус запуска cBackup

## ✅ Проект успешно запущен!

**Дата запуска:** 2025-10-31

### Запущенные сервисы

| Сервис | Статус | Порт | Описание |
|--------|--------|------|----------|
| **cbackup_db** | ✅ Running (healthy) | 3306 | MySQL 8.0 база данных |
| **cbackup_web** | ✅ Running | 9000 (internal) | PHP 8.1-FPM приложение |
| **cbackup_nginx** | ✅ Running | 8080 | Nginx веб-сервер |

### Доступ к приложению

🌐 **Веб-интерфейс:** http://localhost:8080

📊 **База данных:**
- Host: `localhost`
- Port: `3306`
- Database: `cbackup`
- User: `cbackup`
- Password: `cbackup_secure_2024` (см. `.env` файл)

### Команды управления

```bash
# Просмотр статуса
docker-compose ps

# Просмотр логов
docker-compose logs -f

# Остановка
docker-compose stop

# Запуск
docker-compose start

# Перезапуск
docker-compose restart

# Остановка и удаление
docker-compose down
```

### Известные проблемы

⚠️ **Worker (Java сервис)** временно не запущен из-за проблемы с зависимостью `expect4j`:
- Проблема: Зависимость `expect4j` требует недоступные репозитории SpringSource
- Решение: Удалена проблемная зависимость из `pom.xml`
- Статус: Worker нужно будет перекомпилировать после исправления кода, который использует expect4j

**Основная функциональность (веб-интерфейс и база данных) работает!**

### Следующие шаги

1. Откройте http://localhost:8080 в браузере
2. Следуйте инструкциям установщика cBackup
3. Настройте первое устройство для резервного копирования

### Логи

Для просмотра логов каждого сервиса:

```bash
# Логи веб-сервера
docker-compose logs -f web

# Логи базы данных
docker-compose logs -f db

# Логи Nginx
docker-compose logs -f nginx
```

