# 🧹 Полная чистая установка

Для полной чистой установки нужно удалить:
1. Docker volumes (база данных, runtime, redis)
2. Файлы install.lock (локально и в контейнере)
3. Пересоздать контейнеры

## Команды для чистой установки:

```bash
# 1. Остановить все контейнеры
docker compose down

# 2. Удалить ВСЕ volumes (это удалит БД и все данные)
docker compose down -v

# 3. Удалить файлы install.lock (если они есть локально)
rm -f ./core/install.lock ./core/runtime/install.lock ./core/runtime/.install-lock-location

# 4. Пересоздать и запустить контейнеры
docker compose up -d

# 5. Проверить, что установщик открылся
# Откройте браузер: http://localhost:8080
```

## Альтернативный вариант (более безопасный):

```bash
# 1. Остановить контейнеры
docker compose down

# 2. Удалить только volumes базы данных (сохранит runtime если нужно)
docker volume rm cbackup-fork_db_data
docker volume rm cbackup-fork_web_runtime  # опционально
docker volume rm cbackup-fork_redis_data   # опционально

# 3. Удалить install.lock
rm -f ./core/install.lock ./core/runtime/install.lock

# 4. Запустить заново
docker compose up -d
```

## Проверка:

После запуска откройте `http://localhost:8080` - должен появиться мастер установки.

## Добавление дефолтных задач (если отсутствуют):

После установки должны автоматически создаваться дефолтные задачи:
- `backup` - задача резервного копирования
- `discovery` - задача обнаружения устройств
- `stp` - сбор информации STP
- `save` - сохранение в файловую систему
- `git_commit` - коммит в Git
- `log_processing` - обработка логов
- `node_processing` - обработка узлов

Если задачи отсутствуют, выполните:

```bash
docker compose exec web php /var/www/html/add_default_tasks.php
```

Или вручную через SQL:

```bash
docker compose exec db mysql -u cbackup -p cbackup < /path/to/default_tasks.sql
```

