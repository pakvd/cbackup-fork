# 🚀 Автоматическая установка cBackup

Все шаги установки теперь выполняются **автоматически** при запуске контейнеров!

## Автоматические процессы

### 1. Установка прав доступа
При запуске `make up` автоматически выполняется `set-permissions.sh`:
- Создание директории `core/bin`
- Создание `application.properties` с настройками по умолчанию
- Установка правильных прав доступа (755 для директорий, 644 для файлов)

### 2. Установка зависимостей Composer
При запуске контейнера `web` entrypoint скрипт автоматически:
- Проверяет наличие директории `vendor`
- Если директория отсутствует, устанавливает зависимости через `composer install`
- При ошибках автоматически пробует `composer update`
- Проверяет успешность установки

### 3. Создание application.properties
Entrypoint скрипт автоматически:
- Создает директорию `bin` если её нет
- Создает `application.properties` с настройками по умолчанию
- Устанавливает правильные права доступа (www-data:www-data, 644)

## Простая установка (все автоматически)

```bash
# 1. Клонировать репозиторий
git clone <repository-url>
cd cbackup-fork

# 2. Настроить .env (опционально)
cp .env.example .env
# Отредактировать .env при необходимости

# 3. Запустить - все сделается автоматически!
make up

# 4. Открыть веб-интерфейс
# http://localhost:8080
```

Всё! Entrypoint скрипты автоматически установят все зависимости и подготовят окружение.

## Проверка автоматической установки

После запуска `make up` проверьте:

```bash
# Проверить статус контейнеров
make status

# Проверить логи (должны быть сообщения об установке зависимостей)
docker compose logs web | grep -i composer

# Проверить установку зависимостей
make check-deps

# Проверить application.properties
docker compose exec web ls -la /var/www/html/bin/application.properties
```

## Если что-то не установилось автоматически

### Зависимости Composer

```bash
# Установить вручную
make install-composer

# Или через скрипт
./QUICK_FIX_COMPOSER.sh
```

### Права доступа

```bash
# Установить вручную
make set-permissions
```

## Логи для диагностики

```bash
# Логи контейнера web (entrypoint скрипт)
docker compose logs web | grep -E "(Composer|vendor|entrypoint)"

# Проверить установку зависимостей
docker compose exec web test -f /var/www/html/vendor/autoload.php && echo "OK" || echo "ERROR"

# Проверить application.properties
docker compose exec web test -f /var/www/html/bin/application.properties && echo "OK" || echo "ERROR"
```

## Чистая установка (с полной проверкой)

Для полной чистой установки с проверкой всех компонентов:

```bash
./CLEAN_INSTALL.sh
```

Скрипт автоматически:
- Проверит Docker
- Очистит предыдущую установку (при подтверждении)
- Установит права доступа
- Запустит контейнеры
- Подождет и проверит установку зависимостей Composer
- Проверит создание application.properties
- Выведет финальные инструкции

## Что происходит при каждом запуске

1. **Entrypoint скрипт** (`docker-entrypoint.sh`) запускается при старте контейнера `web`
2. **Проверка vendor**: Если директория `vendor` отсутствует, запускается установка зависимостей
3. **Composer install**: Пытается установить зависимости из `composer.lock`
4. **Composer update**: Если `install` не сработал, автоматически пробует `update`
5. **Проверка bin**: Создает директорию `bin` и файл `application.properties` если их нет
6. **Права доступа**: Устанавливает правильные права для `www-data` пользователя
7. **Запуск PHP-FPM**: После всех проверок запускает PHP-FPM

## Преимущества автоматической установки

✅ **Не нужно запускать команды вручную**  
✅ **Все зависимости устанавливаются автоматически**  
✅ **Права доступа настраиваются автоматически**  
✅ **Файлы конфигурации создаются автоматически**  
✅ **Работает при каждом перезапуске контейнера**

## Примечания

- При volume mount (`./core:/var/www/html`) директория `vendor` из образа перезаписывается
- Entrypoint скрипт автоматически устанавливает зависимости при каждом запуске, если `vendor` отсутствует
- Это может занять 1-2 минуты при первом запуске
- Последующие запуски будут быстрее, так как зависимости уже установлены

---

**Дата создания:** 2024-11-02  
**Версия:** 1.0

