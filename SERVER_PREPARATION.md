# 🖥️ Подготовка сервера к установке cBackup

## 📋 Содержание

1. [Системные требования](#системные-требования)
2. [Обновление системы](#обновление-системы)
3. [Установка Docker и Docker Compose](#установка-docker-и-docker-compose)
4. [Настройка Redis memory overcommit](#настройка-redis-memory-overcommit)
5. [Настройка Firewall](#настройка-firewall)
6. [Проверка готовности](#проверка-готовности)

## 📊 Системные требования

### Минимальные требования:
- **ОС**: Linux (Ubuntu 20.04+, Debian 10+, CentOS 7+)
- **RAM**: 4 GB
- **Диск**: 10 GB свободного места
- **CPU**: 2 ядра

### Рекомендуемые требования:
- **ОС**: Ubuntu 22.04 LTS или Debian 11+
- **RAM**: 8 GB
- **Диск**: 20 GB свободного места (SSD предпочтительно)
- **CPU**: 4+ ядра

## 🔄 Обновление системы

### Ubuntu/Debian:

```bash
# Обновить список пакетов
sudo apt-get update

# Обновить систему
sudo apt-get upgrade -y

# Установить необходимые утилиты
sudo apt-get install -y curl wget git
```

### CentOS/RHEL:

```bash
# Обновить систему
sudo yum update -y

# Установить необходимые утилиты
sudo yum install -y curl wget git
```

## 🐳 Установка Docker и Docker Compose

### Установка Docker:

```bash
# Скачать и установить Docker
curl -fsSL https://get.docker.com -o get-docker.sh
sudo sh get-docker.sh

# Добавить текущего пользователя в группу docker
sudo usermod -aG docker $USER

# Включить автозапуск Docker
sudo systemctl enable docker
sudo systemctl start docker

# Проверить установку
docker --version
```

**⚠️ Важно**: После добавления в группу docker, нужно выйти и войти заново, или выполнить:
```bash
newgrp docker
```

### Установка Docker Compose:

```bash
# Скачать последнюю версию Docker Compose
sudo curl -L "https://github.com/docker/compose/releases/latest/download/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker compose

# Сделать исполняемым
sudo chmod +x /usr/local/bin/docker compose

# Проверить установку
docker compose version
```

**Примечание**: Если команда `docker compose` не найдена, используйте `docker-compose` (через дефис) или добавьте в PATH.

## ⚙️ Настройка Redis memory overcommit

Эта настройка устраняет предупреждение Redis и улучшает стабильность при сохранении данных.

### Временно (до перезагрузки):

```bash
sudo sysctl vm.overcommit_memory=1
```

### Постоянно (сохранится после перезагрузки):

```bash
# Добавить в конфигурацию
echo "vm.overcommit_memory = 1" | sudo tee -a /etc/sysctl.conf

# Применить изменения
sudo sysctl -p
```

### Проверка:

```bash
# Проверить текущее значение
sysctl vm.overcommit_memory

# Должно быть: vm.overcommit_memory = 1
```

**После настройки** перезапустите Redis контейнер:
```bash
docker compose restart redis
docker compose logs redis | grep -i warning
```

## 🔥 Настройка Firewall

### Ubuntu/Debian (UFW):

```bash
# Разрешить HTTP порт
sudo ufw allow 8080/tcp

# Разрешить HTTPS порт (если используется)
sudo ufw allow 443/tcp

# Включить firewall
sudo ufw enable

# Проверить статус
sudo ufw status
```

### CentOS/RHEL (firewalld):

```bash
# Разрешить HTTP порт
sudo firewall-cmd --permanent --add-port=8080/tcp

# Разрешить HTTPS порт
sudo firewall-cmd --permanent --add-port=443/tcp

# Применить изменения
sudo firewall-cmd --reload

# Проверить статус
sudo firewall-cmd --list-all
```

### Важно:

- **MySQL порт (3306)** НЕ нужно открывать - он доступен только внутри Docker сети
- **Redis порт (6379)** НЕ нужно открывать - он доступен только внутри Docker сети
- Открывайте только порты для веб-сервера (80/443 или 8080)

## ✅ Проверка готовности

После выполнения всех настроек, проверьте готовность сервера:

```bash
# 1. Проверить Docker
docker --version
docker compose version

# 2. Проверить memory overcommit
sysctl vm.overcommit_memory
# Должно быть: vm.overcommit_memory = 1

# 3. Проверить права пользователя
groups | grep docker
# Должна быть группа docker

# 4. Проверить доступность портов
sudo netstat -tulpn | grep -E '8080|443'
# Порты должны быть свободны (или заняты вашим сервисом)

# 5. Проверить свободное место
df -h
# Должно быть минимум 10 GB свободно
```

## 📝 Чек-лист перед установкой

- [ ] Система обновлена
- [ ] Docker установлен и запущен
- [ ] Docker Compose установлен
- [ ] Пользователь добавлен в группу docker
- [ ] `vm.overcommit_memory = 1` настроен
- [ ] Firewall настроен (порты 8080/443 открыты)
- [ ] Достаточно места на диске (минимум 10 GB)
- [ ] Достаточно RAM (минимум 4 GB)

## 🚀 Следующий шаг

После подготовки сервера переходите к установке:

```bash
# Клонировать репозиторий
git clone <repository-url>
cd cbackup-fork

# Создать .env файл
cp .env.example .env

# Отредактировать пароли
nano .env

# Запустить приложение
docker compose up -d
```

См. также:
- `README.md` - Быстрый старт
- `PRODUCTION_READY.md` - Настройки для production
- `CLEAN_INSTALL.md` - Чистая установка

