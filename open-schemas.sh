#!/bin/bash
# Скрипт для открытия схем в браузере

echo "📊 Открываю схемы cBackup в браузере..."

# Получаем абсолютный путь к файлу
SCHEMAS_FILE="$(cd "$(dirname "$0")" && pwd)/schemas.html"

# Проверяем наличие файла
if [ ! -f "$SCHEMAS_FILE" ]; then
    echo "❌ Файл schemas.html не найден!"
    exit 1
fi

# Определяем команду для открытия браузера в зависимости от ОС
if [[ "$OSTYPE" == "darwin"* ]]; then
    # macOS
    open "$SCHEMAS_FILE"
elif [[ "$OSTYPE" == "linux-gnu"* ]]; then
    # Linux
    if command -v xdg-open &> /dev/null; then
        xdg-open "$SCHEMAS_FILE"
    elif command -v gnome-open &> /dev/null; then
        gnome-open "$SCHEMAS_FILE"
    else
        echo "Пожалуйста, откройте файл вручную: $SCHEMAS_FILE"
    fi
elif [[ "$OSTYPE" == "msys" || "$OSTYPE" == "win32" ]]; then
    # Windows
    start "$SCHEMAS_FILE"
else
    echo "Не удалось определить операционную систему."
    echo "Пожалуйста, откройте файл вручную: $SCHEMAS_FILE"
fi

echo "✅ Схемы открыты в браузере!"
echo ""
echo "💡 Совет: Вы можете сохранить диаграммы как изображения,"
echo "   используя расширение браузера или скриншот."

