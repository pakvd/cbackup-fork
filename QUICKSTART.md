# 🚀 Быстрый старт cBackup

## Самый простой способ запуска

```bash
# 1. Клонировать репозиторий
git clone <repository-url>
cd cbackup-fork

# 2. Настроить переменные окружения
cp .env.example .env
# Отредактировать .env (опционально, есть значения по умолчанию)

# 3. Запустить (автоматически установит права на файлы)
make up

# 4. Открыть в браузере
# http://localhost:8080
```

## Все команды

```bash
make help          # Показать все команды
make up            # Запустить (автоматически установит права)
make down          # Остановить
make restart       # Перезапустить
make logs          # Показать логи
make status        # Показать статус контейнеров
```

## Альтернативные способы запуска

**Через скрипт-обёртку:**
```bash
./docker-compose-up.sh up -d
```

**Напрямую (с ручной установкой прав):**
```bash
./set-permissions.sh && docker compose up -d
```

**Напрямую (без установки прав):**
```bash
docker compose up -d
```

> ⚠️ **Важно**: При первом запуске рекомендуется использовать `make up` или `./docker-compose-up.sh`, так как они автоматически установят правильные права на файлы. Entrypoint скрипт в контейнере также попытается установить права, но при volume mounts это может не сработать из-за прав доступа.

## Проверка

После запуска проверьте статус:
```bash
make status
```

Просмотрите логи:
```bash
make logs
```

## Установка

При первом открытии в браузере вы увидите мастер установки. Следуйте инструкциям на экране.

**Готово!** 🎉
