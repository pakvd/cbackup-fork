# 🚀 Быстрый старт cBackup

## Минимальная установка (3 команды)

### Шаг 1: Создайте файл .env

```bash
cp .env.example .env
```

> **Важно**: Если файл `.env.example` отсутствует, создайте `.env` вручную со следующим содержимым:
> ```env
> NGINX_PORT=8080
> MYSQL_DATABASE=cbackup
> MYSQL_USER=cbackup
> MYSQL_PASSWORD=cbackup_secure_2024
> MYSQL_ROOT_PASSWORD=root_secure_2024
> ```

### Шаг 2: Запустите приложение

```bash
docker-compose up -d
```

Первый запуск займет 2-5 минут (загрузка образов).

### Шаг 3: Откройте в браузере

**http://localhost:8080**

Готово! 🎉

---

## Что дальше?

### Первый запуск

Если это первый запуск, вы увидите установщик cBackup. Следуйте инструкциям на экране.

### Проверка статуса

```bash
# Проверить все сервисы
docker-compose ps

# Должны быть запущены:
# - cbackup_db (MySQL)
# - cbackup_web (PHP)
# - cbackup_nginx (Nginx)
# - cbackup_worker (Java)
```

### Просмотр логов

```bash
# Все логи
docker-compose logs -f

# Конкретный сервис
docker-compose logs -f web
```

---

## Часто задаваемые вопросы

### Q: Как изменить порт?

**A:** Отредактируйте `.env` файл и измените `NGINX_PORT=8080` на нужный порт, затем:

```bash
docker-compose down
docker-compose up -d
```

### Q: Как изменить пароли?

**A:** Отредактируйте `.env` файл и измените:
- `MYSQL_PASSWORD` - пароль для пользователя БД
- `MYSQL_ROOT_PASSWORD` - пароль root для MySQL

Затем перезапустите:

```bash
docker-compose restart db
```

### Q: Порты заняты, что делать?

**A:** Измените порты в `.env`:

```env
NGINX_PORT=8080    # Используйте другой порт
MYSQL_PORT=3307    # Используйте другой порт
```

### Q: Как остановить приложение?

```bash
docker-compose stop
```

### Q: Как полностью удалить и начать заново?

```bash
# Остановить и удалить контейнеры и данные
docker-compose down -v

# Пересобрать и запустить
docker-compose build --no-cache
docker-compose up -d
```

⚠️ **Внимание**: `docker-compose down -v` удалит все данные базы данных!

---

## Проблемы?

### Сервисы не запускаются

```bash
# 1. Проверьте логи
docker-compose logs

# 2. Проверьте статус
docker-compose ps

# 3. Перезапустите
docker-compose restart
```

### Веб-интерфейс не открывается

1. Проверьте, что используете правильный порт: **http://localhost:8080**
2. Проверьте статус: `docker-compose ps`
3. Проверьте логи: `docker-compose logs nginx`

### Ошибки базы данных

```bash
# Перезапустите базу данных
docker-compose restart db

# Проверьте логи
docker-compose logs db
```

---

## Дополнительная информация

- **Полная документация**: [README.md](README.md)
- **История изменений**: [CHANGELOG.md](CHANGELOG.md)
- **Оригинальная документация**: https://github.com/cBackup/main

---

**Время установки**: ~5 минут  
**Сложность**: Легко  
**Требования**: Docker и Docker Compose
