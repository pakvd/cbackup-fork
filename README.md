# cBackup - Network Equipment Configuration Backup Tool

Модернизированная версия cBackup с поддержкой Docker Compose для простого запуска и развертывания.

## 🚀 Быстрый старт

### Вариант 1: Полностью автоматическая установка (рекомендуется)

**Все шаги выполняются автоматически!** Просто запустите:

```bash
# 1. Клонировать репозиторий
git clone <repository-url>
cd cbackup-fork

# 2. Настроить .env (опционально)
cp .env.example .env

# 3. Запустить - все сделается автоматически!
make up

# 4. Открыть веб-интерфейс
# http://localhost:8080
```

**Что происходит автоматически:**
- ✅ Установка прав доступа
- ✅ Установка зависимостей Composer
- ✅ Создание application.properties
- ✅ Настройка всех необходимых директорий

📖 **Подробнее об автоматической установке:** см. [AUTO_INSTALL.md](AUTO_INSTALL.md)

### Вариант 2: Чистая установка с проверкой

Для чистой установки с подробной проверкой всех компонентов:

```bash
# 1. Клонировать репозиторий
git clone <repository-url>
cd cbackup-fork

# 2. Запустить скрипт чистой установки
./CLEAN_INSTALL.sh

# 3. Следовать инструкциям на экране
```

### Вариант 3: Ручная установка (если нужен контроль каждого шага)

```bash
# 1. Клонировать репозиторий
git clone <repository-url>
cd cbackup-fork

# 2. Настроить переменные окружения
cp .env.example .env
# Отредактировать .env при необходимости

# 3. Установить права доступа
make set-permissions

# 4. Запустить контейнеры
docker compose up -d

# 5. Проверить установку зависимостей (автоматически при запуске)
make check-deps

# 6. Если зависимости не установлены, установить вручную:
make install-composer

# 7. Открыть в браузере
# http://localhost:8080

# 8. После веб-установки проверить синхронизацию:
docker compose exec web php yii sync-properties/check
```

📖 **Подробное руководство:** см. [INSTALLATION_GUIDE.md](INSTALLATION_GUIDE.md)

**Все доступные команды Makefile:**
```bash
make help          # Показать все доступные команды
make up            # Запустить контейнеры (автоматически установит права)
make down           # Остановить контейнеры
make restart        # Перезапустить контейнеры
make logs           # Показать логи
make build          # Собрать контейнеры
make set-permissions # Установить права на файлы
```

### Требования

- **Docker** 20.10 или новее
- **Docker Compose** 2.0 или новее
- **Минимум 4GB RAM**
- **Минимум 10GB свободного места**
- **Linux сервер** (Ubuntu 20.04+, Debian 10+, CentOS 7+)

### Подготовка сервера

Перед установкой рекомендуется выполнить настройки сервера:

#### 1. Обновление системы

```bash
# Ubuntu/Debian
sudo apt-get update && sudo apt-get upgrade -y

# CentOS/RHEL
sudo yum update -y
```

#### 2. Установка Docker и Docker Compose

```bash
# Ubuntu/Debian
curl -fsSL https://get.docker.com -o get-docker.sh
sudo sh get-docker.sh
sudo usermod -aG docker $USER
sudo systemctl enable docker
sudo systemctl start docker

# Установка Docker Compose
sudo curl -L "https://github.com/docker/compose/releases/latest/download/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker compose
sudo chmod +x /usr/local/bin/docker compose

# Проверка
docker --version
docker compose version
```

#### 3. Настройка Redis memory overcommit (рекомендуется)

Это устранит предупреждение Redis и улучшит стабильность:

```bash
# Временно (до перезагрузки)
sudo sysctl vm.overcommit_memory=1

# Постоянно (сохранится после перезагрузки)
echo "vm.overcommit_memory = 1" | sudo tee -a /etc/sysctl.conf
sudo sysctl -p
```

#### 4. Настройка Firewall (опционально)

```bash
# Разрешить необходимые порты
sudo ufw allow 8080/tcp  # HTTP
sudo ufw allow 443/tcp   # HTTPS (если используется)
sudo ufw enable

# Или для firewalld (CentOS/RHEL)
sudo firewall-cmd --permanent --add-port=8080/tcp
sudo firewall-cmd --permanent --add-port=443/tcp
sudo firewall-cmd --reload
```

#### 5. Создание пользователя для приложения (опционально)

```bash
# Создать пользователя для cBackup
sudo useradd -m -s /bin/bash cbackup
sudo usermod -aG docker cbackup

# Переключиться на пользователя
su - cbackup
```

**📖 Подробная инструкция**: См. `SERVER_PREPARATION.md`

### Установка и запуск (4 шага)

#### 1. Клонирование репозитория

```bash
git clone <repository-url>
cd cbackup-fork
```

> **Примечание**: Замените `<repository-url>` на URL вашего репозитория или путь к локальной копии проекта.

#### 2. Настройка переменных окружения

```bash
cp .env.example .env
```

Откройте `.env` и при необходимости измените пароли:

```env
MYSQL_PASSWORD=your_secure_password
MYSQL_ROOT_PASSWORD=your_secure_root_password
```

**По умолчанию используются безопасные пароли** (`cbackup_secure_2024` и `root_secure_2024`). Для продакшена обязательно измените их!

#### 3. Запуск приложения

**Рекомендуемый способ (автоматически устанавливает права):**

```bash
# Используя Makefile (рекомендуется)
make up

# Или используя скрипт-обёртку
./docker-compose-up.sh up -d

# Или напрямую (после установки прав вручную)
./set-permissions.sh && docker compose up -d
```

**Альтернативный способ:**

```bash
docker compose up -d
```

> **Примечание**: При первом запуске рекомендуется использовать `make up` или `./docker-compose-up.sh`, так как эти команды автоматически установят правильные права на файлы перед запуском контейнеров.

Первый запуск может занять 2-5 минут (загрузка образов и установка зависимостей).

#### 4. Открыть в браузере

**http://localhost:8080**

> **Примечание**: По умолчанию используется порт **8080**. Если нужно использовать порт 80, измените `NGINX_PORT=80` в `.env` файле.

Если это первый запуск, вы увидите установщик cBackup. Следуйте инструкциям на экране.

### Проверка работоспособности

```bash
# Проверить статус всех сервисов
docker compose ps

# Просмотреть логи
docker compose logs -f

# Проверить конкретный сервис
docker compose logs -f web
docker compose logs -f worker
docker compose logs -f db
```

## 📋 Структура проекта

```
cbackup/
├── core/                 # PHP веб-интерфейс (Yii2 Framework)
│   ├── Dockerfile       # Docker образ для PHP-FPM
│   ├── composer.json    # PHP зависимости
│   └── web/            # Веб-корень приложения
├── worker/              # Java worker сервис (Spring Boot)
│   ├── Dockerfile       # Docker образ для Java
│   └── pom.xml          # Maven зависимости
├── nginx/                # Конфигурация Nginx
│   ├── nginx.conf
│   └── default.conf
├── docker compose.yml    # Основной файл Docker Compose
├── .env.example          # Пример переменных окружения
└── README.md            # Этот файл
```

## 🛠 Управление сервисами

### Остановка

```bash
docker compose stop
```

### Запуск

```bash
docker compose start
```

### Перезапуск

```bash
docker compose restart
```

### Полная остановка с удалением контейнеров

```bash
docker compose down
```

### Очистка данных (⚠️ удалит все данные БД)

```bash
docker compose down -v
```

### Пересборка образов

```bash
docker compose build --no-cache
docker compose up -d
```

## 🔧 Конфигурация

### Порты

По умолчанию используются следующие порты:
- **8080** - Веб-интерфейс (Nginx)
- **3306** - MySQL база данных

Чтобы изменить порты, отредактируйте `.env` файл:

```env
NGINX_PORT=8080    # Порт для веб-интерфейса
MYSQL_PORT=3306    # Порт для MySQL
```

### База данных

Параметры подключения к БД настраиваются в `.env`:

```env
MYSQL_DATABASE=cbackup
MYSQL_USER=cbackup
MYSQL_PASSWORD=your_password
MYSQL_ROOT_PASSWORD=your_root_password
```

Эти же параметры автоматически используются PHP приложением.

## ⚠️ Известные проблемы и исправления

### Ошибка 502 Bad Gateway

Если Nginx не может подключиться к PHP-FPM, проверьте:

```bash
# Проверить PHP-FPM
./CHECK_PHPFPM.sh

# Исправить listen адрес (если PHP-FPM слушает на 127.0.0.1)
./FIX_PHPFPM_LISTEN.sh

# Детальная диагностика
./DEBUG_PHPFPM.sh
```

📖 **Подробнее:** см. [TROUBLESHOOTING_502.md](TROUBLESHOOTING_502.md) и [QUICK_FIX_502.md](QUICK_FIX_502.md)

### Ошибка "Setting unknown property: yii\redis\Connection::socketTimeout"

Исправлено в конфигурации. Если появляется:
- Очистите кеш: `docker compose exec web rm -rf /var/www/html/runtime/cache/*`
- Перезапустите: `docker compose restart web`

### Зависимости Composer не устанавливаются

```bash
# Установить вручную
make install-composer

# Или через скрипт
./QUICK_FIX_COMPOSER.sh
```

📖 **Подробнее:** см. [INSTALL_COMPOSER_DEPS.md](INSTALL_COMPOSER_DEPS.md)

📋 **Все исправления:** см. [CHANGELOG_FIXES.md](CHANGELOG_FIXES.md)

## 🔄 Синхронизация application.properties

Файл `application.properties` синхронизируется автоматически при установке и изменении настроек в веб-интерфейсе. При необходимости можно выполнить ручную синхронизацию:

### Через веб-интерфейс
1. Откройте страницу конфигурации: `http://localhost:8080/index.php?r=config`
2. В секции "Daemon credentials" нажмите кнопку "Синхронизировать application.properties"

### Через консольную команду
```bash
docker compose exec web php yii sync-properties/check
```

Эта команда проверит соответствие значений в БД и файле, исправит права доступа при необходимости и выполнит синхронизацию.

### При проблемах с правами доступа

Если появляется ошибка о правах доступа, выполните:
```bash
docker compose exec web bash -c "chown -R www-data:www-data /var/www/html/bin && chmod 755 /var/www/html/bin && chmod 644 /var/www/html/bin/application.properties"
docker compose restart web
```

Подробнее: см. [FIX_PERMISSIONS.md](FIX_PERMISSIONS.md)

## 🐛 Решение проблем

### Проблема: Порты уже заняты

**Решение**: Измените порты в `.env` файле:

```env
NGINX_PORT=8080    # Используйте другой порт
MYSQL_PORT=3307    # Используйте другой порт
```

Затем перезапустите:

```bash
docker compose down
docker compose up -d
```

### Проблема: Сервисы не запускаются

**Решение**:

1. Проверьте логи:
```bash
docker compose logs
```

2. Проверьте, что порты не заняты:
```bash
# Linux/macOS
netstat -an | grep 8080
netstat -an | grep 3306

# macOS альтернатива
lsof -i :8080
lsof -i :3306
```

3. Пересоберите образы:
```bash
docker compose down
docker compose build --no-cache
docker compose up -d
```

### Проблема: Ошибки подключения к базе данных

**Решение**:

1. Проверьте, что база данных запущена:
```bash
docker compose ps db
```

2. Проверьте логи базы данных:
```bash
docker compose logs db
```

3. Проверьте переменные окружения:
```bash
cat .env | grep MYSQL
```

4. Перезапустите базу данных:
```bash
docker compose restart db
```

### Проблема: Веб-интерфейс не открывается

**Решение**:

1. Проверьте статус всех сервисов:
```bash
docker compose ps
```

2. Проверьте логи Nginx:
```bash
docker compose logs nginx
```

3. Проверьте логи PHP:
```bash
docker compose logs web
```

4. Убедитесь, что используете правильный порт:
   - По умолчанию: **http://localhost:8080**
   - Проверьте значение `NGINX_PORT` в `.env`

### Проблема: Ошибки прав доступа

**Решение**:

```bash
# Исправить права на runtime директории
docker compose exec web chmod -R 775 /var/www/html/runtime
docker compose exec web chmod -R 775 /var/www/html/web/assets

# Проверить владельца файлов
docker compose exec web ls -la /var/www/html/runtime
```

## 📝 Миграции базы данных

Схема базы данных создаётся автоматически при первом запуске через установщик.

Для ручного применения миграций Yii2:

```bash
docker compose exec web php yii migrate
```

## 🔒 Безопасность

### Рекомендации для продакшена

1. **Измените пароли** в `.env` файле перед использованием
2. **Используйте HTTPS** - добавьте SSL сертификаты в конфигурацию Nginx
3. **Ограничьте доступ** к базе данных через firewall
4. **Настройте регулярные бэкапы** базы данных
5. **Обновляйте зависимости** регулярно для исправления уязвимостей

> 📖 **Подробная инструкция для продакшн**: См. [PRODUCTION.md](PRODUCTION.md)

### Настройка HTTPS (опционально)

1. Поместите SSL сертификаты в `nginx/ssl/`
2. Обновите `nginx/default.conf` для поддержки HTTPS
3. Измените `NGINX_SSL_PORT` в `.env` при необходимости

## 📊 Компоненты системы

### Веб-интерфейс (core)
- **Технологии**: PHP 8.1, Yii2 Framework
- **Сервер**: Nginx + PHP-FPM
- **Контейнер**: `cbackup_web`

### Java Worker
- **Технологии**: Java 21, Spring Boot 2.7.18
- **Назначение**: Выполнение задач резервного копирования
- **Контейнер**: `cbackup_worker`

### База данных
- **Технологии**: MySQL 8.0
- **Контейнер**: `cbackup_db`

## 🔄 Обновление

```bash
# 1. Остановите контейнеры
docker compose down

# 2. Обновите код (если используете git)
git pull

# 3. Пересоберите образы
docker compose build --no-cache

# 4. Запустите снова
docker compose up -d

# 5. Примените миграции БД (если есть)
docker compose exec web php yii migrate
```

## 💡 Полезные команды

```bash
# Просмотр логов последние 100 строк
docker compose logs --tail=100

# Просмотр логов конкретного сервиса
docker compose logs -f web

# Выполнение команды в контейнере
docker compose exec web php yii migrate
docker compose exec db mysql -u cbackup -p cbackup

# Просмотр использования ресурсов
docker stats

# Очистка неиспользуемых образов
docker system prune -a
```

## 📚 Дополнительная документация

### Основные руководства
- **🚀 Быстрый старт**: [QUICKSTART.md](QUICKSTART.md)
- **📖 Полная установка**: [INSTALLATION_GUIDE.md](INSTALLATION_GUIDE.md)
- **⚙️ Автоматическая установка**: [AUTO_INSTALL.md](AUTO_INSTALL.md)
- **🚀 Production Deployment**: [PRODUCTION_CHECKLIST.md](PRODUCTION_CHECKLIST.md)

### Решение проблем
- **🔧 Устранение ошибки 502**: [TROUBLESHOOTING_502.md](TROUBLESHOOTING_502.md)
- **⚡ Быстрое исправление 502**: [QUICK_FIX_502.md](QUICK_FIX_502.md)
- **📦 Зависимости Composer**: [INSTALL_COMPOSER_DEPS.md](INSTALL_COMPOSER_DEPS.md)
- **🔐 Права доступа**: [FIX_PERMISSIONS.md](FIX_PERMISSIONS.md)

### Исправления и изменения
- **📋 Журнал исправлений**: [CHANGELOG_FIXES.md](CHANGELOG_FIXES.md)
- **📝 История изменений**: [CHANGELOG.md](CHANGELOG.md)

### Дополнительно
- **Оригинальная документация**: https://github.com/cBackup/main
- **Официальный сайт**: http://cbackup.me

## 🆘 Поддержка

Если вы столкнулись с проблемами:

1. Проверьте логи: `docker compose logs`
2. Проверьте статус сервисов: `docker compose ps`
3. Обратитесь к разделу "Решение проблем" выше
4. Проверьте оригинальную документацию: https://github.com/cBackup/main

## 📄 Лицензия

AGPL-3.0-only

## 👥 Авторы

**Оригинальные разработчики:**
- Oļegs Čapligins
- Imants Černovs
- Dmitrijs Galočkins

**Обновления и модернизация:**
Выполнены для актуализации проекта на 2024-2025 год:
- ✅ Обновление PHP с 7.0 до 8.1
- ✅ Обновление Java с 8 до 21
- ✅ Обновление всех зависимостей
- ✅ Полная контейнеризация с Docker Compose
- ✅ Исправление всех уязвимостей безопасности
- ✅ Модернизация интерфейса

---

**Версия**: 2024.12  
**Последнее обновление**: Декабрь 2024
